# In the root 'SolCipher' directory
git add .
git commit -m "Initial commit: SolCipher project structure and code"

# Create a new repository on GitHub (e.g., named 'SolCipher') and then run:
# Replace <YOUR_GH_USERNAME> and <REPO_NAME> with your details
git remote add origin https://github.com/<YOUR_GH_USERNAME>/<REPO_NAME>.git
git branch -M main
git push -u origin main
```<ctrl63>